# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart==0.0.9

# Resume parsing
pdfplumber==0.11.0
python-docx==1.1.2

# NLP — lightweight, CPU-only, fits Render's 512 MB free tier
spacy==3.8.2
# spaCy model is downloaded in render-build.sh (not a pip package on free tier)

# ML / similarity
scikit-learn==1.5.0
numpy==1.26.4

# Pydantic v2 (bundled with FastAPI 0.111)
pydantic>=2.10.0